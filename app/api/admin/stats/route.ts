import {NextResponse} from "next/server"; import {connectDB} from "@/lib/db"; import {requireSession} from "@/lib/auth"; import User from "@/models/User"; import Payment from "@/models/Payment"; import Investment from "@/models/Investment";
export async function GET(){try{await requireSession("admin");await connectDB();const [users,pending,approved,investments]=await Promise.all([User.countDocuments({role:"user"}),Payment.countDocuments({status:"pending"}),Payment.countDocuments({status:"approved"}),Investment.countDocuments({status:"active"})]);return NextResponse.json({users,pending,approved,investments})}catch{return NextResponse.json({error:"Unauthorized"},{status:401})}}
