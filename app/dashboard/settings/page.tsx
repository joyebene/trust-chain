"use client";
import {useEffect,useState} from "react"; import {UserRound,ShieldCheck} from "lucide-react";
export default function Settings(){const [u,setU]=useState<any>(null);useEffect(()=>{fetch("/api/me").then(r=>r.json()).then(d=>setU(d.user))},[]);return <div className="dashboard-page"><div className="container-app"><div className="welcome-row"><div><span className="section-kicker">ACCOUNT</span><h1>Settings</h1><p className="muted">Your account information and security details.</p></div></div><div className="settings-grid"><div className="panel"><div className="panel-head"><div><h3>Profile</h3><p>Information associated with your account.</p></div></div><div className="profile-line"><UserRound/><div><span>Full name</span><b>{u?.name||"—"}</b></div></div><div className="profile-line"><UserRound/><div><span>Email</span><b>{u?.email||"—"}</b></div></div><div className="profile-line"><UserRound/><div><span>Phone</span><b>{u?.phone||"—"}</b></div></div></div><div className="panel"><div className="panel-head"><div><h3>Security</h3><p>Account protection.</p></div></div><div className="security-box"><ShieldCheck/><div><b>Secure session</b><p>Your authentication session is stored in an HTTP-only cookie.</p></div></div></div></div></div></div>}
