"use client";
import {useEffect,useState} from "react"; import {TrendingUp} from "lucide-react"; import {money,dateTime} from "@/lib/format";
export default function Investments(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{fetch("/api/investments").then(r=>r.json()).then(d=>setRows(d.investments||[]))},[]);return <div className="dashboard-page"><div className="container-app"><div className="welcome-row"><div><span className="section-kicker">PORTFOLIO</span><h1>Your investments</h1><p className="muted">Review your approved investment positions and terms.</p></div></div><div className="panel table-panel"><div className="table-scroll"><table><thead><tr><th>Investment</th><th>Amount</th><th>Cycle</th><th>Target</th><th>Status</th><th>Started</th></tr></thead><tbody>{rows.map(i=><tr key={i._id}><td><div className="table-name"><div className="tiny-icon"><TrendingUp/></div><b>{i.currency} investment</b></div></td><td>{money(i.amount,i.currency==="BTC"?"USD":i.currency)}</td><td>{i.cycleDaysMin}–{i.cycleDaysMax} days</td><td>Up to {i.targetReturnPercent}%*</td><td><span className={`status ${i.status}`}>{i.status}</span></td><td>{dateTime(i.startedAt)}</td></tr>)}</tbody></table></div>{!rows.length&&<div className="empty">No investments yet. Start from your dashboard when you are ready.</div>}<div className="risk-note">*Target returns are not guaranteed. Review the applicable investment agreement and risk disclosures before relying on any projection.</div></div></div></div>}
